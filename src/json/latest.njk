---
permalink: allwomenstalk.com/latest.json
---

{
  "items": [
    {%- for post in posts |slicearr (50) -%}
    {
      "title": "{{ post.title }}",
      "image": "{{ post.image }}",
      "url": "{{ post.url }}",
      "category": "{{ post.category }}"
    }{% if not loop.last %},{% endif %}
    {%- endfor -%}
  ]
}