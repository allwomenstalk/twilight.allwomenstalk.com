---
layout: layout/default.njk
pagination:
  data: posts
  size: 1
  alias: post
permalink: /{{post.host}}/{{post.slug}}/
url: https://allwomenstalk.com/index.html
amptitle: "{{title}}"
ampurl: "{{url}}"
ampimage: "{{image}}"
ampcategory: "{{category}}"
eleventyComputed: 
  title: "{{post.title}} - {{metadata.title}}"
---

{% include 'partials/leaderboard.njk' %}
{% include 'partials/herojumbo.njk' %}
{# {% include 'partials/widget.njk' %} #}
{# {% include 'partials/shopping.njk' %} #}
<main id="content" class="bg-gray-50">
  {% for page in post.pages %}
    <article class="post prose p-4 overflow-hidden mx-auto max-w-xl {% if post.noaff %} _noaff {% endif %} {% if loop.first %}
        first-line:uppercase first-letter:text-7xl first-letter:font-bold first-letter:mr-3 first-letter:float-left {%- endif
        -%}">
      {% if page.title %}
        <h3 id="{{ loop.index0 }}" class="not-prose text-3xl md:text-4xl font-bold my-4 md:my-6">
          {% if page.number %}
            {{ page.number }}.
          {% endif %}
          {{ page.title | safe }}
        </h3>
      {% endif %}
      {{ page.pageContent | enhanceLists | safe }}
    </article>
    {% include 'partials/elaborate.njk' %}
    {% if not loop.first and not loop.last %}
      {% include 'partials/faq.njk' %}
    {% endif %}
    
    {% if not loop.last %}
      {# inline related #}
      {% if loop.index > 1 %}
        {% set inline = post.related.posts[loop.index-2] %}
        {# {% include "partials/relatedinline.njk" %} #}
      {% endif %}
      {% if loop.index == 1 %}
        {% include 'partials/poll.njk' %}
        {% include 'partials/seperator.njk' %}
        {% include 'partials/toc.njk' %}
      {% elif loop.index == 2 %}
        {% include 'partials/seperator.njk' %}
      {% elif loop.index == 3 %}
        {# {% include 'partials/faq.njk' %} #}
        {% if post.faq %}
          {# default show faq and extra ad unit #}
          {# {% include 'partials/seperator.njk' %} #}
        {% elif post.related.inline %}
          {% include 'partials/seperator.njk' %}
        {% else %}
          {% include 'partials/seperator.njk' %}
          {% if quizzes[post.id] %}
            {% include "partials/quiz.njk" %}
          {% else %}
            {% include 'partials/poll.njk' %}
          {% endif %}
        {% endif %}
      {% elif loop.index == 4 %}
        {% include 'partials/seperator.njk' %}
        {# 5 #}
      {% elif (loop.index == 5) %}
        {% if post.related.inline %}
          {% include "partials/relatedinline.njk" %}
        {% endif %}
      {% elif (loop.index == 6) %}
        {# {% include "partials/videos.njk" %} #}
        {% include 'partials/seperator.njk' %}
      {% elif (loop.index == 10) %}
        {# {% include 'partials/story.njk' %} #}
        {% include 'partials/seperator.njk' %}
      {% elif (loop.index == 15 ) %}
        {# {% include "partials/explore.njk" %} #}
        {# {% include 'partials/seperator.njk' %} #}
      {% elif (loop.index == 10) and (loop.length > 12) %}
        {# adding shopping at the middle on the long posts #}
        {% include 'partials/shopping.njk' %}
      {% else %}
        {# <hr class="ad-slot hidden hr{{loop.index}}"> #}
        {% include 'partials/seperator.njk' %}
      {% endif %}
      {# {% include 'partials/cta.njk' %} #}
    {% else %}
      {% include 'partials/poll.njk' %}
      {# {% include 'partials/pinterestcard.njk' %} #}
      {# {% include 'partials/newsletter.njk' %} #}
        {% if post.videos and post.videos.length > 0 %}
        {% from "partials/videosgrid.njk" import videosGrid %}
      {{ videosGrid(true) }}
      {% endif %}
      {# adding shopping on the end for short posts #}
      {# {% include 'partials/subscribe.njk' %} #}
      {# {% include 'partials/rating.njk' %} #}
      {% include "partials/commentspanel.njk" %}
      {% if (loop.length < 12) %}
        {# {% include 'partials/shopping.njk' %} #}
      {% endif %}
      {# {% include 'partials/collections.njk' %} #}
      {# {% include 'partials/seperator.njk' %} #}
      {% if not post.noads %}
        {# {% include 'partials/keywordsgallery.njk' %} #}
      {% endif %}
    {% endif %}
  {% endfor %}
  {% include 'partials/google.njk'%}
  {% include "partials/heroapp.njk" %}
  {% include 'partials/herogamification.njk' %}
</main>
{% include 'partials/products.njk' %}
{% include 'partials/iniret.njk' %}
{% set modal = true %}
{% set samplesize = 5 %}
{# {% include 'partials/infeedity/feed.njk' %} #}
{% include 'partials/relatedbadges.njk' %}
{% include 'partials/adsnew.njk' %}
<div class="ad-slot mx-auto overflow-hidden" id="slot-related"></div>
{% include 'partials/ads.njk' %}{# {% include "partials/listmix.njk" %} #}
{# {% include 'partials/popular.njk' %} #}{% include 'partials/schema.njk' %}
{# <div class="ad-slot mx-auto overflow-hidden" id="slot-review"></div> #}
{# {% include 'partials/reviews.njk' %} #}<div class="ad-slot mx-auto overflow-hidden" id="slot-final"> </div>
{# Posts scripts only  #}
{% if post.amp %}
  <script async src="https://cdn.ampproject.org/v0.js"></script>
{% endif %}
{% if post.ampig %}
  <script async custom-element="amp-instagram" src="https://cdn.ampproject.org/v0/amp-instagram-0.1.js"></script>
{% endif %}
<script type='text/javascript'>
  (function () {
    var loc = window.location.href;
    var dd = document.createElement('script');
    dd.type = 'text/javascript';
    dd.src = 'https://static.digidip.net/allwomenstalk.js?loc=' + loc;
    var s = document.getElementsByTagName('script')[0];
    s.parentNode.insertBefore(dd, s);
  })();
</script>
{# faq single script  #}
<script>
  function faqData() {
    return {
      {% if post.faq %}
        items : {{ post.faq | dump | safe }},
      {%- else -%}
        items : [],
      {%- endif -%}
      shown: 0,
      maxShown: 3,
      observer: null,
      loadTimer: null,
      loaded: false,
      revealTimer: null,
      async init() {
        console.log('faq', this.items)
        if (this.items.length > 0) {
          this.shuffleItems();
        }
        this.observeHeading();
      },
      revealItems() {
        if (this.revealTimer) return;
        const total = this.items ? this.items.length : 0;
        if (total === 0) return;
        this.shuffleItems();
        const targetCount = Math.min(this.maxShown, total);
        this.revealTimer = setInterval(() => {
          if (this.shown >= targetCount) {
            clearInterval(this.revealTimer);
            this.revealTimer = null;
            return;
          }
          this.shown += 1;
        }, 500);
      },
      shuffleItems() {
        if (!this.items || this.items.length < 2) return;
        for (let i = this.items.length - 1; i > 0; i -= 1) {
          const j = Math.floor(Math.random() * (i + 1));
          const temp = this.items[i];
          this.items[i] = this.items[j];
          this.items[j] = temp;
        }
      },
      observeHeading() {
        if (this.loaded || this.observer) return;
        const target = this.$el || null;
        if (!target || !window.IntersectionObserver) {
          if (this.items.length > 0) {
            this.revealItems();
          } else {
            this.loadData();
          }
          return;
        }
        this.observer = new IntersectionObserver((entries) => {
          const entry = entries[0];
          if (!entry) return;
          if (entry.isIntersecting) {
            if (this.loadTimer) return;
            this.loadTimer = setTimeout(() => {
              this.loaded = true;
              this.loadTimer = null;
              if (this.items.length > 0) {
                this.revealItems();
              } else {
                this.loadData();
              }
              if (this.observer) this.observer.disconnect();
            }, 1000);
          } else if (this.loadTimer) {
            clearTimeout(this.loadTimer);
            this.loadTimer = null;
          }
        }, { threshold: 0.6 });
        this.observer.observe(target);
      },
      async loadData() {
        const cdnUrl = 'https://pub-1969d309989b4ee98dadd29698bd2213.r2.dev/{{ post.id }}.json';
        const workerUrl = 'https://faq.azo.workers.dev/{{ post.id }}/';

        try {
          const cdnResponse = await fetch(cdnUrl);
          if (cdnResponse.ok) {
            const cdnData = await cdnResponse.json();
            console.log('faq', cdnData);
            this.items = cdnData.list || [];
            this.shuffleItems();
            this.revealItems();
            return;
          }
          if (cdnResponse.status !== 404) {
            console.warn('FAQ CDN request failed:', cdnResponse.status);
            return;
          }
        } catch (error) {
          console.warn('FAQ CDN error:', error);
        }

        try {
          const response = await fetch(workerUrl);
          if (!response.ok) {
            console.warn('FAQ worker request failed:', response.status);
            return;
          }
          const data = await response.json();
          console.log('faq', data);
          this.items = data.list || [];
          this.shuffleItems();
          this.revealItems();
        } catch (error) {
          console.error('FAQ worker error:', error);
        }
      },
      report(index) {
        this.shown = Math.min(this.shown, this.maxShown);
        item = this.items[index];
        item.index = index;
        item.total = this.items.length;
        this.shown ++;
        beacontrack({type: 'faq', action: 1, post_id: '{{ post.id }}', item: item})
      },
      askQuestion() {
        const question = document
          .querySelector('input[x-model="question"]')
          .value
          .trim();
        if (! question) 
          return;
        
        const postUrl = '{{ post.url }}';
        const siteDomain = 'allwomenstalk.com';
        const prompt = `Answer question: ${question}, in context of ${postUrl}, link back to article and suggest more relevant articles from ${siteDomain}`;
        const chatUrl = `https://chat.openai.com/?hints=search&q=${
          encodeURIComponent(prompt)
        }`;
        window.open(chatUrl, '_blank');
        // Track the question
        const item = {
          question: question,
          index: -1, // Special index for user questions
          total: (this.items ?. length ?? 0) + 1,
          isAI: true, // Mark as AI-generated question
          source: 'user_input' // Indicate this came from user input
        };
        beacontrack({type: 'faq', action: 1, post_id: '{{ post.id }}', item: item});
      }
    };
  }
</script>
